services:
  - type: web
    name: nemotron-workspace
    env: node
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: PUBLIC_APP_URL
        sync: false
